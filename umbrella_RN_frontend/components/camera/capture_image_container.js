import { connect } from 'react-redux';
import CaptureImage from './capture_image';

const mapSTPs = () => ({

});

// will need action to save photo
const mapDTPs = (dispatch) => ({

});

export default connect(mapSTPs, mapDTPs)(CaptureImage);
